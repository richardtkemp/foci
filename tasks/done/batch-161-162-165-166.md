# Task Batch: #161, #162, #165, #166

Do these in order. Commit each separately. Push after each commit. Update SPEC.md and docs/CONFIG.md where relevant.

---

## 1. #161 — Tmux kill caution reminder in tool description

Add a caution note to the tmux tool's `kill` operation description: "Before killing: is follow-up likely? Loaded context is expensive to rebuild. Ask before killing coding agent sessions."

This is just a tool description string change — no logic change.

---

## 2. #162 — Telegram typing indicator during all activity

The Telegram "typing..." indicator should show during all activity — tool calls, waiting for tool results, thinking — not just when composing the reply text. Currently it only appears when writing the final message.

Send the "typing" chat action at the start of processing and keep it alive throughout the agent's turn (tool loop, thinking, etc.), not just at the text composition stage.

---

## 3. #165 — [INLINECODE<] rendering bug

Bug: inline code (backticks) sometimes renders as `[INLINECODE<]` in Telegram messages instead of proper `<code>` tags. Likely a bug in the markdown-to-Telegram-HTML converter. 

Find where this string is generated, understand the conversion logic, and fix it so backtick-wrapped text renders correctly as inline code in Telegram HTML.

---

## 4. #166 — Spawn: give all context modes tools

Currently only `clone_current` spawn mode has tools. Change it so all context modes (none, character_only, clone_current) get tool access.

Blacklist `send_telegram` and `send_to_session` from "none" mode — no character context means no awareness of communication conventions. All other tools are fine for all modes.
