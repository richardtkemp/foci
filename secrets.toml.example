# Foci Secrets — keep this file restricted (chmod 660, owned by root:foci-secrets)
#
# Place in the same directory as foci.toml. Values here override matching
# fields in foci.toml. setup.sh handles permissions automatically.

# ─── LLM Provider API Keys ──────────────────────────────────────────
# Configure the API key for your chosen provider.
# Only one is needed — whichever you selected during `foci first-run`.

# [anthropic]
# api_key = "sk-ant-api03-..."

# [gemini]
# api_key = "AI..."

# [openai]
# api_key = "sk-..."

# [openrouter]
# api_key = "sk-or-..."

# ─── Telegram Bot Tokens ──────────────────────────────────────────────
# Keys under [telegram] match agent IDs or bot names in foci.toml.
# Token resolved by convention: agent with telegram_bot = "myagent" looks up telegram.myagent.
[telegram]
myagent = "123456789:AAF-YOUR_BOT_TOKEN_HERE"

# ─── Optional: Additional Secrets ─────────────────────────────────────
# Available as {{secret:NAME}} in http_request headers/body.
# Dot-separated keys become nested TOML sections.
# allowed_hosts restricts which hosts a secret can be sent to via http_request.
#
# [brave]
# api_key = "BSA..."            # Brave Search API (for web_search tool)
#
# [github]
# token = "ghp_..."
# allowed_hosts = ["api.github.com"]
#
# [groq]
# api_key = "..."               # Groq API key (used for STT/TTS via secret = "groq.api_key")
